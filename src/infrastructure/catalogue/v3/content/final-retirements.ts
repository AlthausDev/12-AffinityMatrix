/**
 * Entries retired during the final content-quality pass. These are kept separate from earlier
 * semantic retirements so the reason and stage of removal remain auditable.
 */
export const FINAL_CONTENT_RETIRED_PRACTICE_IDS = new Set<string>([
  'full-body-massage',
]);
